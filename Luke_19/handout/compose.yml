name: juleportal
services:
  juleportal:
    build: .
    ports:
      - "8080:8080"
    environment:
      DATABASE_STI: /data/juleportal.db
      NETTSIDE_STI: /app/nettside
      PORT: "8080"
    restart: unless-stopped